/** @type {import('next').NextConfig} */
const nextConfig = {
  // appDir é habilitado por padrão no Next.js 13+
  images: {
    domains: ['images.pokemontcg.io'],
  },
}

module.exports = nextConfig
